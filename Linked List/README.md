# 📌 Singly Linked List Implementation in Java

This project provides a complete **Singly Linked List** implementation in **Java**, covering all the fundamental and advanced operations commonly asked in **DSA interviews** and academic courses.

The code is written in a clean, structured, and beginner-friendly way, making it ideal for **learning, practice, and revision**.

---

## 🚀 Features Implemented

### 🔹 Insertion Operations
- Add node at the beginning (`addFirst`)
- Add node at a specific index (`add`)
- Add node at the end (`addLast`)

### 🔹 Deletion Operations
- Remove first node (`removeFirst`)
- Remove last node (`removeLast`)
- Delete Nth node from the end (`deleteNthfromEnd`)

### 🔹 Searching
- Iterative search (`itrSearch`)
- Recursive search (`recSearch`)

### 🔹 Reversal
- Reverse the linked list (`reverse`)

### 🔹 Palindrome Check
- Check whether the linked list is a palindrome (`checkPalindrome`)

### 🔹 Cycle Detection & Removal
- Detect cycle using **Floyd’s Cycle Detection Algorithm** (`cycle`)
- Remove cycle from the linked list (`removeCycle`)

### 🔹 Utility Methods
- Find middle of the linked list (`findMid`)
- Print linked list (`printList`)

---

## 🧠 Algorithms Used

- **Slow & Fast Pointer Technique**
- **Floyd’s Cycle Detection Algorithm**
- **In-place Linked List Reversal**
- **Recursive Backtracking for Search**

---
